#include<stdio.h>
int main()
{
int x,y,z;
printf("Enter the value of x and y");
scanf("%d%d",&x,&y);
if
    ((x*x-y)>(y*y-x))
    printf("true");
    else
        printf("false");
return 0;
}
